# Server-role drop-in for xymonlaunch.service, shipped only by the
# xymon package. Kill xymonlaunch but not its children on stop: xymond
# and its workers may be writing checkpoints and flushing caches, and
# must be allowed to disconnect on their own.

[Service]
KillMode=process
SendSIGKILL=no
